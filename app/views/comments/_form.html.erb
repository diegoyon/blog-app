<%= form_with model: [ @post.author, @post, @post.comments.build ], class: 'comment-form' do |form| %>
  <%= form.label :text %>
  <%= form.text_field :text, class: 'comment-input', placeholder: "Write a comment..." %>
  <%= form.submit "Comment", class: 'btn' %>
<% end %>